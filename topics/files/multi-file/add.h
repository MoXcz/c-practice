#ifndef ADD_H
#define ADD_H // this variable helps make sure that the header file is only
              // included once

// will only be added if 'add.h' has not been included already
int add(int, int);

#endif
